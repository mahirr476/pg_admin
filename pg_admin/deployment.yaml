# # deployment.yaml - Deploy to your existing pgaks cluster
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-server
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-server
#   template:
#     metadata:
#       labels:
#         app: pg-admin-server
#     spec:
#       containers:
#       - name: server
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 7000
#         env:
#         - name: NODE_ENV
#           value: "production"
#         - name: PORT
#           value: "7000"
#         - name: JWT_SECRET
#           value: "paragon-admin-secret"
#         - name: DATABASE_URL_GLOBAL
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/globaladmin?sslmode=require"
#         - name: DATABASE_URL_GROUP
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/groupdb?sslmode=require"
#         - name: DATABASE_URL_PARASOLE
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/parasoledb?sslmode=require"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             apk add --no-cache git
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             cd server
#             npm install
#             npx prisma generate --schema=./prisma/global/schema.prisma
#             npx prisma generate --schema=./prisma/group/schema.prisma
#             npx prisma generate --schema=./prisma/parasole/schema.prisma
#             npx prisma migrate deploy --schema=./prisma/global/schema.prisma
#             npx prisma migrate deploy --schema=./prisma/group/schema.prisma
#             npx prisma migrate deploy --schema=./prisma/parasole/schema.prisma
#             npm start
#         resources:
#           requests:
#             memory: "512Mi"
#             cpu: "250m"
#           limits:
#             memory: "1Gi"
#             cpu: "500m"
# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-client
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-client
#   template:
#     metadata:
#       labels:
#         app: pg-admin-client
#     spec:
#       containers:
#       - name: client
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 3000
#         env:
#         - name: NODE_ENV
#           value: "production"
#         - name: NEXT_PUBLIC_API_URL
#           value: "http://pg-admin-server-service:7000"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             apk add --no-cache git
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             cd client
#             npm install --legacy-peer-deps
#             npm run build
#             npm start
#         resources:
#           requests:
#             memory: "512Mi"
#             cpu: "250m"
#           limits:
#             memory: "1Gi"
#             cpu: "500m"
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-server-service
# spec:
#   selector:
#     app: pg-admin-server
#   ports:
#   - port: 7000
#     targetPort: 7000
#   type: LoadBalancer
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-client-service
# spec:
#   selector:
#     app: pg-admin-client
#   ports:
#   - port: 80
#     targetPort: 3000
#   type: LoadBalancer



# deployment.yaml - Fixed version
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-server
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-server
#   template:
#     metadata:
#       labels:
#         app: pg-admin-server
#     spec:
#       containers:
#       - name: server
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 7000
#         env:
#         - name: NODE_ENV
#           value: "production"
#         - name: PORT
#           value: "7000"
#         - name: JWT_SECRET
#           value: "paragon-admin-secret"
#         - name: DATABASE_URL_GLOBAL
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/globaladmin?sslmode=require"
#         - name: DATABASE_URL_GROUP
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/groupdb?sslmode=require"
#         - name: DATABASE_URL_PARASOLE
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/parasoledb?sslmode=require"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             apk add --no-cache git
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             ls -la
#             find . -name "package.json" -type f
#             find . -name "*.prisma" -type f
#             if [ -d "server" ]; then
#               cd server
#             elif [ -d "backend" ]; then
#               cd backend
#             elif [ -f "package.json" ]; then
#               echo "Found package.json in root directory"
#             else
#               echo "Directory structure:"
#               find . -maxdepth 3 -type d
#               exit 1
#             fi
#             npm install
#             if [ -f "prisma/global/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/global/schema.prisma
#               npx prisma migrate deploy --schema=./prisma/global/schema.prisma
#             fi
#             if [ -f "prisma/group/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/group/schema.prisma
#               npx prisma migrate deploy --schema=./prisma/group/schema.prisma
#             fi
#             if [ -f "prisma/parasole/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/parasole/schema.prisma
#               npx prisma migrate deploy --schema=./prisma/parasole/schema.prisma
#             fi
#             npm start
#         resources:
#           requests:
#             memory: "512Mi"
#             cpu: "250m"
#           limits:
#             memory: "1Gi"
#             cpu: "500m"
# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-client
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-client
#   template:
#     metadata:
#       labels:
#         app: pg-admin-client
#     spec:
#       containers:
#       - name: client
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 3000
#         env:
#         - name: NODE_ENV
#           value: "production"
#         - name: NEXT_PUBLIC_API_URL
#           value: "http://pg-admin-server-service:7000"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             apk add --no-cache git
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             ls -la
#             find . -name "package.json" -type f
#             if [ -d "client" ]; then
#               cd client
#             elif [ -d "frontend" ]; then
#               cd frontend
#             elif [ -f "package.json" ]; then
#               echo "Found package.json in root directory"
#             else
#               echo "Directory structure:"
#               find . -maxdepth 3 -type d
#               exit 1
#             fi
#             npm install --legacy-peer-deps
#             npm run build
#             npm start
#         resources:
#           requests:
#             memory: "512Mi"
#             cpu: "250m"
#           limits:
#             memory: "1Gi"
#             cpu: "500m"
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-server-service
# spec:
#   selector:
#     app: pg-admin-server
#   ports:
#   - port: 7000
#     targetPort: 7000
#   type: LoadBalancer
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-client-service
# spec:
#   selector:
#     app: pg-admin-client
#   ports:
#   - port: 80
#     targetPort: 3000
#   type: LoadBalancer





# deployment.yaml - Fixed version
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-server
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-server
#   template:
#     metadata:
#       labels:
#         app: pg-admin-server
#     spec:
#       containers:
#       - name: server
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 7000
#         env:
#         - name: NODE_ENV
#           value: "production"
#         - name: PORT
#           value: "7000"
#         - name: JWT_SECRET
#           value: "paragon-admin-secret"
#         - name: DATABASE_URL_GLOBAL
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/globaladmin?sslmode=require"
#         - name: DATABASE_URL_GROUP
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/groupdb?sslmode=require"
#         - name: DATABASE_URL_PARASOLE
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/parasoledb?sslmode=require"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             apk add --no-cache git
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             ls -la
#             find . -name "package.json" -type f
#             find . -name "*.prisma" -type f
#             cd pg_admin/server
#             npm install
#             if [ -f "prisma/global/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/global/schema.prisma
#               npx prisma migrate deploy --schema=./prisma/global/schema.prisma
#             fi
#             if [ -f "prisma/group/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/group/schema.prisma
#               npx prisma migrate deploy --schema=./prisma/group/schema.prisma
#             fi
#             if [ -f "prisma/parasole/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/parasole/schema.prisma
#               npx prisma migrate deploy --schema=./prisma/parasole/schema.prisma
#             fi
#             npm start
#         resources:
#           requests:
#             memory: "512Mi"
#             cpu: "250m"
#           limits:
#             memory: "1Gi"
#             cpu: "500m"
# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-client
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-client
#   template:
#     metadata:
#       labels:
#         app: pg-admin-client
#     spec:
#       containers:
#       - name: client
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 3000
#         env:
#         - name: NODE_ENV
#           value: "production"
#         - name: NEXT_PUBLIC_API_URL
#           value: "http://pg-admin-server-service:7000"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             apk add --no-cache git
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             ls -la
#             find . -name "package.json" -type f
#             cd pg_admin/client
#             npm install --legacy-peer-deps
#             npm run build
#             npm start
#         resources:
#           requests:
#             memory: "512Mi"
#             cpu: "250m"
#           limits:
#             memory: "1Gi"
#             cpu: "500m"
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-server-service
# spec:
#   selector:
#     app: pg-admin-server
#   ports:
#   - port: 7000
#     targetPort: 7000
#   type: LoadBalancer
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-client-service
# spec:
#   selector:
#     app: pg-admin-client
#   ports:
#   - port: 80
#     targetPort: 3000
#   type: LoadBalancer





# # direct-ts-deployment.yaml - Run TypeScript directly with ts-node
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-server
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-server
#   template:
#     metadata:
#       labels:
#         app: pg-admin-server
#     spec:
#       containers:
#       - name: server
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 7000
#         env:
#         - name: NODE_ENV
#           value: "development"
#         - name: PORT
#           value: "7000"
#         - name: JWT_SECRET
#           value: "paragon-admin-secret"
#         - name: DATABASE_URL_GLOBAL
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/globaladmin?sslmode=require"
#         - name: DATABASE_URL_GROUP
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/groupdb?sslmode=require"
#         - name: DATABASE_URL_PARASOLE
#           value: "postgresql://pgwebadmin:pg%40webadmin25@pgwebpostgresql.postgres.database.azure.com:5432/parasoledb?sslmode=require"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             echo "=== DIRECT TYPESCRIPT DEPLOYMENT ==="
            
#             # Install git and clone repository
#             apk add --no-cache git
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             cd pg_admin/server
            
#             echo "=== INSTALLING DEPENDENCIES ==="
#             npm install
            
#             echo "=== GENERATING PRISMA CLIENTS ==="
#             # Generate Prisma clients only (skip migrations)
#             if [ -f "prisma/global/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/global/schema.prisma || echo "Global schema generation failed"
#             fi
#             if [ -f "prisma/group/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/group/schema.prisma || echo "Group schema generation failed"
#             fi
#             if [ -f "prisma/parasole/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/parasole/schema.prisma || echo "Parasole schema generation failed"
#             fi
            
#             echo "=== STARTING TYPESCRIPT SERVER DIRECTLY ==="
#             # Use npm run dev which uses ts-node (no build required)
#             npm run dev
#         resources:
#           requests:
#             memory: "1Gi"
#             cpu: "500m"
#           limits:
#             memory: "2Gi"
#             cpu: "1000m"
# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-client
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-client
#   template:
#     metadata:
#       labels:
#         app: pg-admin-client
#     spec:
#       containers:
#       - name: client
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 3000
#         env:
#         - name: NODE_ENV
#           value: "production"
#         - name: NEXT_PUBLIC_API_URL
#           value: "http://pg-admin-server-service:7000"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             echo "=== STARTING CLIENT DEPLOYMENT ==="
            
#             # Install git and clone repository  
#             apk add --no-cache git
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             cd pg_admin/client
            
#             echo "=== INSTALLING CLIENT DEPENDENCIES ==="
#             npm install --legacy-peer-deps
            
#             echo "=== BUILDING CLIENT ==="
#             npm run build
            
#             echo "=== STARTING CLIENT ==="
#             npm start
#         resources:
#           requests:
#             memory: "1Gi"
#             cpu: "500m"
#           limits:
#             memory: "2Gi"
#             cpu: "1000m"
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-server-service
# spec:
#   selector:
#     app: pg-admin-server
#   ports:
#   - port: 7000
#     targetPort: 7000
#   type: LoadBalancer
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-client-service
# spec:
#   selector:
#     app: pg-admin-client
#   ports:
#   - port: 80
#     targetPort: 3000
#   type: LoadBalancer





# # direct-ts-deployment.yaml - Run TypeScript directly with ts-node
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-server
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-server
#   template:
#     metadata:
#       labels:
#         app: pg-admin-server
#     spec:
#       containers:
#       - name: server
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 7000
#         env:
#         - name: NODE_ENV
#           value: "development"
#         - name: PORT
#           value: "7000"
#         - name: JWT_SECRET
#           value: "paragon-admin-secret"
#         - name: DATABASE_URL_GLOBAL
#           value: "postgresql://pgwebadmin:Pg%40Admin2025%21@pgwebpostgresql.postgres.database.azure.com:5432/globaladmin?sslmode=require"
#         - name: DATABASE_URL_GROUP
#           value: "postgresql://pgwebadmin:Pg%40Admin2025%21@pgwebpostgresql.postgres.database.azure.com:5432/groupdb?sslmode=require"
#         - name: DATABASE_URL_PARASOLE
#           value: "postgresql://pgwebadmin:Pg%40Admin2025%21@pgwebpostgresql.postgres.database.azure.com:5432/parasoledb?sslmode=require"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             echo "=== DIRECT TYPESCRIPT DEPLOYMENT ==="
            
#             # Install git, openssl (for Prisma), and other dependencies
#             apk add --no-cache git openssl openssl-dev
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             cd pg_admin/server
            
#             echo "=== INSTALLING DEPENDENCIES ==="
#             npm install
            
#             echo "=== GENERATING PRISMA CLIENTS ==="
#             # Generate Prisma clients only (skip migrations)
#             if [ -f "prisma/global/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/global/schema.prisma || echo "Global schema generation failed"
#             fi
#             if [ -f "prisma/group/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/group/schema.prisma || echo "Group schema generation failed"
#             fi
#             if [ -f "prisma/parasole/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/parasole/schema.prisma || echo "Parasole schema generation failed"
#             fi
            
#             echo "=== STARTING TYPESCRIPT SERVER DIRECTLY ==="
#             # Use npm run dev which uses ts-node (no build required)
#             npm run dev
#         resources:
#           requests:
#             memory: "1Gi"
#             cpu: "500m"
#           limits:
#             memory: "2Gi"
#             cpu: "1000m"
# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-client
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-client
#   template:
#     metadata:
#       labels:
#         app: pg-admin-client
#     spec:
#       containers:
#       - name: client
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 3000
#         env:
#         - name: NODE_ENV
#           value: "production"
#         - name: NEXT_PUBLIC_API_URL
#           value: "http://pg-admin-server-service:7000"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             echo "=== STARTING CLIENT DEPLOYMENT ==="
            
#             # Install git, openssl (for Prisma), and other dependencies  
#             apk add --no-cache git openssl openssl-dev
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             cd pg_admin/client
            
#             echo "=== INSTALLING CLIENT DEPENDENCIES ==="
#             npm install --legacy-peer-deps
            
#             echo "=== BUILDING CLIENT ==="
#             npm run build
            
#             echo "=== STARTING CLIENT ==="
#             npm start
#         resources:
#           requests:
#             memory: "1Gi"
#             cpu: "500m"
#           limits:
#             memory: "2Gi"
#             cpu: "1000m"
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-server-service
# spec:
#   selector:
#     app: pg-admin-server
#   ports:
#   - port: 7000
#     targetPort: 7000
#   type: LoadBalancer
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-client-service
# spec:
#   selector:
#     app: pg-admin-client
#   ports:
#   - port: 80
#     targetPort: 3000
#   type: LoadBalancer




# deployment.yaml - Complete fixed version
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-server
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-server
#   template:
#     metadata:
#       labels:
#         app: pg-admin-server
#     spec:
#       containers:
#       - name: server
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 7000
#         env:
#         - name: NODE_ENV
#           value: "development"
#         - name: PORT
#           value: "7000"
#         - name: JWT_SECRET
#           value: "paragon-admin-secret"
#         - name: DATABASE_URL_GLOBAL
#           value: "postgresql://pgwebadmin:Pg%40Admin2025%21@pgwebpostgresql.postgres.database.azure.com:5432/globaladmin?sslmode=require"
#         - name: DATABASE_URL_GROUP
#           value: "postgresql://pgwebadmin:Pg%40Admin2025%21@pgwebpostgresql.postgres.database.azure.com:5432/groupdb?sslmode=require"
#         - name: DATABASE_URL_PARASOLE
#           value: "postgresql://pgwebadmin:Pg%40Admin2025%21@pgwebpostgresql.postgres.database.azure.com:5432/parasoledb?sslmode=require"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             echo "=== STARTING SERVER DEPLOYMENT ==="
            
#             # Install git, openssl (for Prisma), and other dependencies
#             apk add --no-cache git openssl openssl-dev
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             cd pg_admin/server
            
#             echo "=== INSTALLING DEPENDENCIES ==="
#             npm install
            
#             echo "=== GENERATING PRISMA CLIENTS ==="
#             # Generate Prisma clients (skip migrations to avoid DB connection issues)
#             if [ -f "prisma/global/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/global/schema.prisma || echo "Global schema generation failed"
#             fi
#             if [ -f "prisma/group/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/group/schema.prisma || echo "Group schema generation failed"
#             fi
#             if [ -f "prisma/parasole/schema.prisma" ]; then
#               npx prisma generate --schema=./prisma/parasole/schema.prisma || echo "Parasole schema generation failed"
#             fi
            
#             echo "=== STARTING TYPESCRIPT SERVER DIRECTLY ==="
#             # Use npm run dev which uses ts-node (no build required)
#             npm run dev
#         resources:
#           requests:
#             memory: "1Gi"
#             cpu: "500m"
#           limits:
#             memory: "2Gi"
#             cpu: "1000m"
# ---
# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: pg-admin-client
#   namespace: default
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: pg-admin-client
#   template:
#     metadata:
#       labels:
#         app: pg-admin-client
#     spec:
#       containers:
#       - name: client
#         image: node:18-alpine
#         workingDir: /app
#         ports:
#         - containerPort: 3000
#         env:
#         - name: NODE_ENV
#           value: "development"
#         - name: NEXT_PUBLIC_API_URL
#           value: "http://4.144.193.232:7000"
#         command: ["/bin/sh"]
#         args:
#           - -c
#           - |
#             echo "=== STARTING CLIENT DEPLOYMENT ==="
            
#             # Install git, openssl (for Prisma), and other dependencies  
#             apk add --no-cache git openssl openssl-dev
#             git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
#             cd pg_admin/client
            
#             echo "=== INSTALLING CLIENT DEPENDENCIES ==="
#             npm install --legacy-peer-deps
            
#             echo "=== STARTING CLIENT IN DEV MODE ==="
#             # Skip build, run in development mode to avoid build errors
#             npm run dev
#         resources:
#           requests:
#             memory: "1Gi"
#             cpu: "500m"
#           limits:
#             memory: "2Gi"
#             cpu: "1000m"
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-server-service
# spec:
#   selector:
#     app: pg-admin-server
#   ports:
#   - port: 7000
#     targetPort: 7000
#   type: LoadBalancer
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pg-admin-client-service
# spec:
#   selector:
#     app: pg-admin-client
#   ports:
#   - port: 80
#     targetPort: 3000
#   type: LoadBalancer





# deployment.yaml - Complete fixed version with correct API URL
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pg-admin-server
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pg-admin-server
  template:
    metadata:
      labels:
        app: pg-admin-server
    spec:
      containers:
      - name: server
        image: node:18-alpine
        workingDir: /app
        ports:
        - containerPort: 7000
        env:
        - name: NODE_ENV
          value: "development"
        - name: PORT
          value: "7000"
        - name: JWT_SECRET
          value: "paragon-admin-secret"
        - name: DATABASE_URL_GLOBAL
          value: "postgresql://pgwebadmin:Pg%40Admin2025%21@pgwebpostgresql.postgres.database.azure.com:5432/globaladmin?sslmode=require"
        - name: DATABASE_URL_GROUP
          value: "postgresql://pgwebadmin:Pg%40Admin2025%21@pgwebpostgresql.postgres.database.azure.com:5432/groupdb?sslmode=require"
        - name: DATABASE_URL_PARASOLE
          value: "postgresql://pgwebadmin:Pg%40Admin2025%21@pgwebpostgresql.postgres.database.azure.com:5432/parasoledb?sslmode=require"
        command: ["/bin/sh"]
        args:
          - -c
          - |
            echo "=== STARTING SERVER DEPLOYMENT ==="
            
            # Install git, openssl (for Prisma), and other dependencies
            apk add --no-cache git openssl openssl-dev
            git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
            cd pg_admin/server
            
            echo "=== INSTALLING DEPENDENCIES ==="
            npm install
            
            echo "=== GENERATING PRISMA CLIENTS ==="
            # Generate Prisma clients (skip migrations to avoid DB connection issues)
            if [ -f "prisma/global/schema.prisma" ]; then
              npx prisma generate --schema=./prisma/global/schema.prisma || echo "Global schema generation failed"
            fi
            if [ -f "prisma/group/schema.prisma" ]; then
              npx prisma generate --schema=./prisma/group/schema.prisma || echo "Group schema generation failed"
            fi
            if [ -f "prisma/parasole/schema.prisma" ]; then
              npx prisma generate --schema=./prisma/parasole/schema.prisma || echo "Parasole schema generation failed"
            fi
            
            echo "=== STARTING TYPESCRIPT SERVER DIRECTLY ==="
            # Use npm run dev which uses ts-node (no build required)
            npm run dev
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pg-admin-client
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pg-admin-client
  template:
    metadata:
      labels:
        app: pg-admin-client
    spec:
      containers:
      - name: client
        image: node:18-alpine
        workingDir: /app
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "development"
        - name: NEXT_PUBLIC_API_URL
          value: "http://20.212.136.46:7000"  # Updated to correct server IP
        command: ["/bin/sh"]
        args:
          - -c
          - |
            echo "=== STARTING CLIENT DEPLOYMENT ==="
            
            # Install git, openssl (for Prisma), and other dependencies  
            apk add --no-cache git openssl openssl-dev
            git clone --single-branch --branch 40/review-and-update-for-all-panel https://github.com/mahirr476/pg_admin.git .
            cd pg_admin/client
            
            # IMPORTANT: Remove any .env files that might override our settings
            rm -f .env .env.local .env.development .env.production
            
            # Create a new .env.local file with our environment variable
            echo "NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL" > .env.local
            
            echo "=== CHECKING ENVIRONMENT ==="
            echo "NEXT_PUBLIC_API_URL is set to: $NEXT_PUBLIC_API_URL"
            cat .env.local
            
            echo "=== INSTALLING CLIENT DEPENDENCIES ==="
            npm install --legacy-peer-deps
            
            echo "=== FIXING HARDCODED LOCALHOST IN ALL FILES ==="
            # More aggressive replacement approach
            echo "Searching and replacing localhost:7000..."
            find /app/pg_admin/client -type f \( -name "*.tsx" -o -name "*.ts" -o -name "*.js" -o -name "*.jsx" \) -exec grep -l "localhost:7000" {} \; | while read file; do
              echo "Processing: $file"
              sed -i "s|http://localhost:7000|${NEXT_PUBLIC_API_URL}|g" "$file"
              sed -i "s|https://localhost:7000|${NEXT_PUBLIC_API_URL}|g" "$file"
              # Also handle cases with quotes
              sed -i "s|'http://localhost:7000'|'${NEXT_PUBLIC_API_URL}'|g" "$file"
              sed -i "s|\"http://localhost:7000\"|\"${NEXT_PUBLIC_API_URL}\"|g" "$file"
            done
            
            echo "=== Verifying replacement ==="
            echo "Files still containing localhost:7000:"
            grep -r "localhost:7000" /app/pg_admin/client --include="*.tsx" --include="*.ts" || echo "None found - replacement successful!"
            
            echo "=== STARTING CLIENT IN DEV MODE ==="
            # Skip build, run in development mode to avoid build errors
            npm run dev
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: pg-admin-server-service
spec:
  selector:
    app: pg-admin-server
  ports:
  - port: 7000
    targetPort: 7000
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  name: pg-admin-client-service
spec:
  selector:
    app: pg-admin-client
  ports:
  - port: 80
    targetPort: 3000
  type: LoadBalancer